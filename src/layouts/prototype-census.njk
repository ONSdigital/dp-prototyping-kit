<!DOCTYPE html>
<html lang="en">

    <head>
        <meta name="robots" content="noindex nofollow"> {# As these are prototypes these should not be indexed #}
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ title or metadata.title }}</title>
        <link rel="stylesheet" type="text/css" href="https://cdn.ons.gov.uk/sixteens/{{ metadata.sixteensVersion }}/css/main.css">
        <link rel="shortcut icon" href="/assets/images/favicon.ico" type="image/x-icon">
        <link href="https://fonts.googleapis.com/css?family=Roboto+Slab" rel="stylesheet">
        <meta name="robots" content="noindex, nofollow">

        {% if page.map == true %}
            <script src='https://api.mapbox.com/mapbox-gl-js/v1.13.0/mapbox-gl.js'></script>
            <link href='https://api.mapbox.com/mapbox-gl-js/v1.13.0/mapbox-gl.css' rel='stylesheet'/>
        {% endif %}

        <link rel="stylesheet" href="/assets/app.bundle.css?version={{ version }}">
        <script defer src="/assets/app.bundle.js?version={{ version }}"></script>

        {% block head %}{% endblock %}
    </head>

    <body>

        {# {% if page.warning != false %}
            {% include "banners/warning.njk" %}
        {% endif %} #}
{# Census banner include #}
        {%- include "banners/census-promo.njk" -%}
        
        {% if page.header != false %}
            {% include "header.njk" %}
        {% endif %}

        {% block body %}{% endblock %}

        {% if page.footer != false %}
            {% include "footer.njk" %}
        {% endif %}

    </body>

    <!--[if gt IE 8]><!-->
    <script type="text/javascript" src="https://cdn.ons.gov.uk/sixteens/{{ metadata.sixteensVersion }}/js/main.js"></script>
    <!--<![endif]-->

</html>